<Page
    x:Class="MainApp.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource mainViewModelLocator}, Path=LoginViewModel}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SplitViewStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Username:" Width="100px" VerticalAlignment="Center"/>
                <TextBox x:Name="userNameTxt" Width="200px" Text="{Binding Path=CurrentEmployee.Username}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Password:" Width="100px" VerticalAlignment="Center"/>
                <PasswordBox x:Name="passWordTxt" Width="200px" Password="{Binding Path=CurrentEmployee.Password}"/>
            </StackPanel>
            <Button Command="{Binding LoginCommand}" Content="Aanmelden" HorizontalAlignment="Center" Width="300px" Margin="0,20,0,0" Style="{StaticResource PageButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>
