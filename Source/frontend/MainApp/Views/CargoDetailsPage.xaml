<Page
    x:Class="MainApp.Views.CargoDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource mainViewModelLocator}, Path=CargoDetailsViewModel}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SplitViewStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="LightGray">
        <StackPanel Orientation="Vertical" VerticalAlignment="Top">
            <StackPanel Orientation="Vertical" Margin="10, 10, 10, 10">
                <Grid VerticalAlignment="Top" x:Name="titleGrid">
                    <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Margin="100, 10, 0, 10" Foreground="White"> Cargo</TextBlock>
                </Grid>
            </StackPanel>
            <Grid Background="White" BorderBrush="Black" BorderThickness="2" Margin="100, 0, 100, 0" Padding="20" VerticalAlignment="Top">
                <StackPanel Orientation="Vertical" Width="600px">
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Width="100"> Cargo:</TextBlock>
                        <TextBlock Width="500" Text="{Binding CurrentCargo.Cargo_id}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Width="100"> Gewicht:</TextBlock>
                        <TextBlock Width="500" Text="{Binding CurrentCargo.Weight}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
            <StackPanel Orientation="Vertical" VerticalAlignment="Top">
                <StackPanel Orientation="Vertical" Margin="10, 10, 10, 10">
                    <Grid VerticalAlignment="Top">
                        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Margin="100, 10, 0, 10" Foreground="White"> Borders</TextBlock>
                    </Grid>
                </StackPanel>
                <ListView ItemsSource="{Binding CurrentCargo.Borders}" SelectedItem="{Binding SelectedBorder, Mode=TwoWay}" Background="White" BorderBrush="Black" Margin="20,20,20,20" BorderThickness="2" MaxHeight="150">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Cargo_border_id}" Width="100px" HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding Variable.Description}" Width="250px" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Value}" Width="250px" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Grid Background="White" BorderBrush="Black" BorderThickness="2" Margin="100, 0, 100, 0" Padding="20">
                    <StackPanel Orientation="Vertical" Width="600px">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Width="100"> ID:</TextBlock>
                            <TextBlock x:Name="idTxt" Width="500" Text="{Binding Path=SelectedBorder.Cargo_border_id}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Width="100"> Beschrijving:</TextBlock>
                            <TextBox x:Name="weightTxt" Width="500" Text="{Binding Path=SelectedBorder.Variable.Description, Mode=TwoWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Style="{StaticResource BaseTextBlockStyle}" VerticalAlignment="Center" Width="100"> Waarde:</TextBlock>
                            <TextBox Width="500" Text="{Binding SelectedBorder.Value, Mode=TwoWay}"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
                <Grid Padding="20" x:Name="buttonGrid">
                    <Button Style="{StaticResource PageButtonStyle}" Command="{Binding UpdateCommand}" Padding="12" HorizontalAlignment="Left" Margin="20,0,0,0" Width="125">Update</Button>
                    <Button Style="{StaticResource PageButtonStyle}" Command="{Binding AddBorderCommand}" Padding="12" HorizontalAlignment="Right" Margin="0,0,0,0" Width="125">Add border</Button>
                </Grid>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Page>
