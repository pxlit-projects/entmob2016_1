<ContentDialog
    x:Class="MainApp.Views.AddVariableDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add Variable"
    PrimaryButtonText="Add"
    SecondaryButtonText="Cancel"
    PrimaryButtonCommand="{Binding AddCommand}"
    PrimaryButtonCommandParameter="{Binding ElementName=addVariableContentDialog}"
    SecondaryButtonCommand="{Binding CancelCommand}"
    DataContext="{Binding Source={StaticResource mainViewModelLocator}, Path=AddVariableViewModel}">

    <Grid>
        <StackPanel>
            
            <TextBox Text="{Binding CurrentVariable.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="Description"/>



        </StackPanel>
    </Grid>
</ContentDialog>
